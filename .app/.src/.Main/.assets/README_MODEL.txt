NOX MODEL PLACEHOLDER

This directory intentionally does not contain the LLM model.
For deployment, install a compatible MediaPipe .task model into:
<app internal storage>/models/nox-model.task

See NOX_LOCAL_SETUP.md.
